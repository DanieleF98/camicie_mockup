const Duration defaultAnimationDuration = Duration(milliseconds: 500);

// MODEL DETAIL SCREEN
const int twoColumns = 2;

// CUSTOM SLIDER
const double minSliderValue = 0.0;
const double maxSliderValue = 100.0;

// QUANTITY PICKER DIALOG
const int minQuantity = 0;
const int maxQuantity = 1000;

// FABRIC IMAGE SCALE
const double maxFabricImageScale = 5;
const double minFabricImageScale = 0.5;

enum ModelOfShirtEnum {
  classic,
  classicSlim,
  slim,
}
